# Снапшоты (192 МБ, не нужны на хостинге)
_снапшоты/

# Служебные tools (генераторы PDF/HTML, не нужны в публикации)
_tools/

# RU-only артефакты, не для публичного показа (хранятся в репо только локально)
_не_публиковать/

# Рабочие/черновые файлы и бэкапы — не публикуются
_рабочее/
_design-system/_logo_backup_2026-07-16/

# MVP — отдельное приложение (backend+frontend), не часть статик-сайта dpfsd.ru.
# Развёртывается своим сервером; в публичный репозиторий сайта не идёт.
18_MVP/

# Скрипты обёрток переводов (служебные)
_translations/_batch7_wrap.ps1
_translations/_batch8_wrap.ps1

# OS / Editor
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Node (если появится)
node_modules/
package-lock.json

# Build artefacts
*.log
*.tmp

# Temp Office locks + one-off helper
~$*
_dpfsd_fix_www.ps1
_arhiv/
*.rar
*.zip
*.7z
*.iso
